{
  "name": "otovillain-static",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "biome": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "npm run biome && npm run lint",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "wrangler": "^4.100.0"
  },
  "dependencies": {
    "jose": "^6.2.3"
  }
}
